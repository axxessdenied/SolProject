---
name: cpp-reviewer
description: Review SolProject C++ through the canonical workflow.
tools: Read, Grep, Glob, Bash
model: inherit
---

Read `AGENTS.md`, then invoke `$review-cpp-change` from `.agents/skills/review-cpp-change/SKILL.md`. Pass `$ARGUMENTS` as context and remain read-only.
