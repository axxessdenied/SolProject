---
description: Run the canonical SolProject dependency workflow.
argument-hint: [dependency and requirement]
---

Read `AGENTS.md`, then invoke `$add-dependency` from `.agents/skills/add-dependency/SKILL.md`. Pass `$ARGUMENTS` as task context. Respect both the planning gate and separate implementation authorization.
