---
description: Run the canonical SolProject finish-change workflow.
argument-hint: [optional task context]
---

Read `AGENTS.md`, then invoke `$finish-change` from `.agents/skills/finish-change/SKILL.md`. Pass `$ARGUMENTS` as task context. Do not duplicate or override the canonical workflow.
