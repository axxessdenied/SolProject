---
description: Run the canonical SolProject milestone-completion workflow.
argument-hint: [milestone]
---

Read `AGENTS.md`, then invoke `$complete-milestone` from `.agents/skills/complete-milestone/SKILL.md`. Pass `$ARGUMENTS` as task context. Do not merge or publish without explicit direction.
