---
description: Run the canonical SolProject pre-PR workflow.
argument-hint: [optional task context]
---

Read `AGENTS.md`, then invoke `$pre-pr` from `.agents/skills/pre-pr/SKILL.md`. Pass `$ARGUMENTS` as task context. PR publication remains user-directed.
