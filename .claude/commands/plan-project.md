---
description: Run the canonical SolProject planning workflow.
argument-hint: [optional planning context]
---

Read `AGENTS.md`, then invoke `$plan-project` from `.agents/skills/plan-project/SKILL.md`. Pass `$ARGUMENTS` as task context. Do not duplicate or override the canonical workflow.
