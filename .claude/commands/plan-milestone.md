---
description: Scope a milestone through the canonical SolProject workflow.
argument-hint: [roadmap item or milestone]
---

Read `AGENTS.md`, then invoke `$plan-milestone` from `.agents/skills/plan-milestone/SKILL.md`. Pass `$ARGUMENTS` as task context. Planning does not authorize implementation.
