// Generated by cmake/SolToolchainFacts.cmake at configure time. Do not edit.
//
// These constants describe the build that produced the binary reading them. They are the
// provenance half of every P1a measurement report.

#pragma once

namespace sol::proto::facts {

/// Git commit the source tree was configured from, or "unknown" if Git was unavailable.
inline constexpr const char* kGitCommit = "@SOL_GIT_COMMIT@";

/// True when the working tree had uncommitted changes at configure time. Evidence runs
/// must record this; a dirty tree makes a measurement unreproducible.
inline constexpr bool kGitDirty = @SOL_GIT_DIRTY@;

/// CMake configure preset name, supplied by CMakePresets.json.
inline constexpr const char* kBuildPreset = "@SOL_BUILD_PRESET@";

/// CMAKE_BUILD_TYPE for this single-configuration tree (ADR 0001).
inline constexpr const char* kBuildType = "@CMAKE_BUILD_TYPE@";

inline constexpr const char* kCompilerId = "@CMAKE_CXX_COMPILER_ID@";
inline constexpr const char* kCompilerVersion = "@CMAKE_CXX_COMPILER_VERSION@";

/// Literal C++23 standard-selection flag CMake emitted for this toolset. Recorded because
/// MSVC has historically spelled this differently across releases.
inline constexpr const char* kCxxStandardFlag = "@SOL_CXX_STANDARD_FLAG@";

/// Full compile-option string applied to project-owned targets.
inline constexpr const char* kCxxFlags = "@SOL_FULL_CXX_FLAGS@";

/// How ADR 0010's no-FMA-contraction requirement is satisfied on this toolset:
/// "explicit-flag" when a disabling flag exists, "implicit-off-under-fp-precise" when the
/// toolset offers only the enabling /fp:contract and the default must be verified instead.
inline constexpr const char* kFpContractionMode = "@SOL_FP_CONTRACTION_MODE@";

/// The explicit contraction-disabling flag, or the empty string when none exists.
inline constexpr const char* kFpContractionFlag = "@SOL_FP_CONTRACTION_FLAG@";

/// True when this build deliberately violates ADR 0010 to serve as a negative control.
/// A build with this set must never be used to produce evidence.
inline constexpr bool kNegativeControlContract =
#if defined(SOL_NEGATIVE_CONTROL_CONTRACT_ENABLED)
    true;
#else
    false;
#endif

} // namespace sol::proto::facts
